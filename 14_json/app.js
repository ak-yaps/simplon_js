var cap = {name : "New Era", edition: "One Piece Edition", price: 79, disponibility: false};

function convertTOJSON(p) {
  return JSON.stringify(p);
}

console.log(p);
console.log(convertTOJSON(cap));
